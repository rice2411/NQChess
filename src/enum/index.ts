export enum EGender {
  MALE = "male",
  FEMALE = "female",
  OTHER = "other",
}

export enum EClassStatus {
  NOT_STARTED = "not_started",
  IN_PROGRESS = "in_progress",
  COMPLETED = "completed",
}

export enum ETuitionStatus {
  PENDING = "pending",
  PAID = "paid",
  OVERDUE = "overdue",
}
